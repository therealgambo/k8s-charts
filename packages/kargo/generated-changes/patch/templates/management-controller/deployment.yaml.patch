--- charts-original/templates/management-controller/deployment.yaml
+++ charts/templates/management-controller/deployment.yaml
@@ -8,7 +8,10 @@
   labels:
     {{- include "kargo.labels" . | nindent 4 }}
     {{- include "kargo.managementController.labels" . | nindent 4 }}
-  {{- with (mergeOverwrite (deepCopy .Values.global.labels) .Values.managementController.labels) }}
+  # kargo.labels above already renders global.labels/extraNamespaceLabels -- re-merging it here
+  # too was a latent duplicate-key bug that only surfaces once that value is actually set
+  # (kyverno-policy-fix).
+  {{- with .Values.managementController.labels }}
     {{- range $key, $value := . }}
     {{ $key }}: {{ $value | quote }}
     {{- end }}
@@ -42,6 +45,7 @@
       {{- end }}
     spec:
       serviceAccountName: kargo-management-controller
+      automountServiceAccountToken: {{ .Values.managementController.automountServiceAccountToken }}
       {{- with .Values.managementController.affinity | default .Values.global.affinity }}
       affinity:
         {{- toYaml . | nindent 8 }}
