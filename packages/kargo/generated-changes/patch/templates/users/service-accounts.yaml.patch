--- charts-original/templates/users/service-accounts.yaml
+++ charts/templates/users/service-accounts.yaml
@@ -7,10 +7,11 @@
   labels:
     {{- include "kargo.labels" . | nindent 4 }}
     rbac.kargo.akuity.io/system-role: "true"
+  {{- $annotations := dict }}
   {{- if .Values.api.oidc.admins.claims }}
-  annotations:
-    rbac.kargo.akuity.io/claims: {{ .Values.api.oidc.admins.claims | toJson | quote }}
+  {{- $annotations = set $annotations "rbac.kargo.akuity.io/claims" (.Values.api.oidc.admins.claims | toJson) }}
   {{- end }}
+  {{- include "kargo.annotations" (dict "root" . "annotations" $annotations) | nindent 2 }}
 ---
 apiVersion: v1
 kind: ServiceAccount
@@ -20,10 +21,11 @@
   labels:
     {{- include "kargo.labels" . | nindent 4 }}
     rbac.kargo.akuity.io/system-role: "true"
+  {{- $annotations := dict }}
   {{- if .Values.api.oidc.projectCreators.claims }}
-  annotations:
-    rbac.kargo.akuity.io/claims: {{ .Values.api.oidc.projectCreators.claims | toJson | quote }}
+  {{- $annotations = set $annotations "rbac.kargo.akuity.io/claims" (.Values.api.oidc.projectCreators.claims | toJson) }}
   {{- end }}
+  {{- include "kargo.annotations" (dict "root" . "annotations" $annotations) | nindent 2 }}
 ---
 apiVersion: v1
 kind: ServiceAccount
@@ -33,10 +35,11 @@
   labels:
     {{- include "kargo.labels" . | nindent 4 }}
     rbac.kargo.akuity.io/system-role: "true"
+  {{- $annotations := dict }}
   {{- if .Values.api.oidc.users.claims }}
-  annotations:
-    rbac.kargo.akuity.io/claims: {{ .Values.api.oidc.users.claims | toJson | quote }}
+  {{- $annotations = set $annotations "rbac.kargo.akuity.io/claims" (.Values.api.oidc.users.claims | toJson) }}
   {{- end }}
+  {{- include "kargo.annotations" (dict "root" . "annotations" $annotations) | nindent 2 }}
 ---
 apiVersion: v1
 kind: ServiceAccount
@@ -46,8 +49,9 @@
   labels:
     {{- include "kargo.labels" . | nindent 4 }}
     rbac.kargo.akuity.io/system-role: "true"
+  {{- $annotations := dict }}
   {{- if .Values.api.oidc.viewers.claims }}
-  annotations:
-    rbac.kargo.akuity.io/claims: {{ .Values.api.oidc.viewers.claims | toJson | quote}}
+  {{- $annotations = set $annotations "rbac.kargo.akuity.io/claims" (.Values.api.oidc.viewers.claims | toJson) }}
   {{- end }}
+  {{- include "kargo.annotations" (dict "root" . "annotations" $annotations) | nindent 2 }}
 {{- end }}
