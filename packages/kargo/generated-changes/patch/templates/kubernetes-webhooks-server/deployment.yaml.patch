--- charts-original/templates/kubernetes-webhooks-server/deployment.yaml
+++ charts/templates/kubernetes-webhooks-server/deployment.yaml
@@ -8,7 +8,10 @@
   labels:
     {{- include "kargo.labels" . | nindent 4 }}
     {{- include "kargo.kubernetesWebhooksServer.labels" . | nindent 4 }}
-  {{- with (mergeOverwrite (deepCopy .Values.global.labels) .Values.webhooksServer.labels) }}
+  # kargo.labels above already renders global.labels/extraNamespaceLabels -- re-merging it here
+  # too was a latent duplicate-key bug that only surfaces once that value is actually set
+  # (kyverno-policy-fix).
+  {{- with .Values.webhooksServer.labels }}
     {{- range $key, $value := . }}
     {{ $key }}: {{ $value | quote }}
     {{- end }}
@@ -46,6 +49,7 @@
       {{- end }}
     spec:
       serviceAccountName: kargo-webhooks-server
+      automountServiceAccountToken: {{ .Values.webhooksServer.automountServiceAccountToken }}
       {{- with .Values.webhooksServer.affinity | default .Values.global.affinity }}
       affinity:
         {{- toYaml . | nindent 8 }}
