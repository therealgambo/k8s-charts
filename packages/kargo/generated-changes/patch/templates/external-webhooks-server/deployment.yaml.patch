--- charts-original/templates/external-webhooks-server/deployment.yaml
+++ charts/templates/external-webhooks-server/deployment.yaml
@@ -8,7 +8,10 @@
   labels:
     {{- include "kargo.labels" . | nindent 4 }}
     {{- include "kargo.externalWebhooksServer.labels" . | nindent 4 }}
-  {{- with (mergeOverwrite (deepCopy .Values.global.labels) .Values.externalWebhooksServer.labels) }}
+  # kargo.labels above already renders global.labels/extraNamespaceLabels -- re-merging it here
+  # too was a latent duplicate-key bug that only surfaces once that value is actually set
+  # (kyverno-policy-fix).
+  {{- with .Values.externalWebhooksServer.labels }}
     {{- range $key, $value := . }}
     {{ $key }}: {{ $value | quote }}
     {{- end }}
@@ -46,6 +49,7 @@
       {{- end }}
     spec:
       serviceAccountName: kargo-external-webhooks-server
+      automountServiceAccountToken: {{ .Values.externalWebhooksServer.automountServiceAccountToken }}
       {{- with .Values.externalWebhooksServer.affinity | default .Values.global.affinity }}
       affinity:
         {{- toYaml . | nindent 8 }}
