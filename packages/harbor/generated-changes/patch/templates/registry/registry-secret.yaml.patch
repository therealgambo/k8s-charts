--- charts-original/templates/registry/registry-secret.yaml
+++ charts/templates/registry/registry-secret.yaml
@@ -6,6 +6,8 @@
   namespace: {{ .Release.Namespace | quote }}
   labels:
 {{ include "harbor.labels" . | indent 4 }}
+  annotations:
+{{ include "harbor.requiredAnnotations" . | indent 4 }}
 type: Opaque
 data:
   {{- if not .Values.registry.existingSecret }}
@@ -47,6 +49,8 @@
   namespace: {{ .Release.Namespace | quote }}
   labels:
 {{ include "harbor.labels" . | indent 4 }}
+  annotations:
+{{ include "harbor.requiredAnnotations" . | indent 4 }}
 type: Opaque
 data:
   {{- if .Values.registry.credentials.htpasswdString }}
