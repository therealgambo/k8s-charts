--- charts-original/templates/portal/deployment.yaml
+++ charts/templates/portal/deployment.yaml
@@ -7,6 +7,8 @@
 {{ include "harbor.labels" . | indent 4 }}
     component: portal
     app.kubernetes.io/component: portal
+  annotations:
+{{ include "harbor.requiredAnnotations" . | indent 4 }}
 spec:
   replicas: {{ .Values.portal.replicas }}
   revisionHistoryLimit: {{ .Values.portal.revisionHistoryLimit }}
@@ -63,9 +65,16 @@
       - name: portal
         image: {{ .Values.portal.image.repository }}:{{ .Values.portal.image.tag }}
         imagePullPolicy: {{ .Values.imagePullPolicy }}
-{{- if .Values.portal.resources }}
         resources:
+{{- if .Values.portal.resources }}
 {{ toYaml .Values.portal.resources | indent 10 }}
+{{- else }}
+          requests:
+            cpu: 100m
+            memory: 256Mi
+          limits:
+            cpu: 1000m
+            memory: 1024Mi
 {{- end }}
 {{- with .Values.portal.extraEnvVars }}
         env:
