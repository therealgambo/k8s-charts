--- charts-original/charts/spiffe-oidc-discovery-provider/templates/pre-delete-hook.yaml
+++ charts/charts/spiffe-oidc-discovery-provider/templates/pre-delete-hook.yaml
@@ -7,6 +7,7 @@
   labels:
     {{- include "spiffe-oidc-discovery-provider.labels" . | nindent 4 }}
   annotations:
+    {{- include "spiffe-oidc-discovery-provider.patchedAnnotations" . | nindent 4 }}
     "helm.sh/hook": pre-delete
     "helm.sh/hook-delete-policy": before-hook-creation, hook-succeeded, hook-failed
 ---
@@ -15,7 +16,10 @@
 metadata:
   name: {{ include "spiffe-oidc-discovery-provider.fullname" . }}-pre-delete
   namespace: {{ include "spiffe-oidc-discovery-provider.namespace" . }}
+  labels:
+    {{- include "spiffe-oidc-discovery-provider.labels" . | nindent 4 }}
   annotations:
+    {{- include "spiffe-oidc-discovery-provider.patchedAnnotations" . | nindent 4 }}
     "helm.sh/hook": pre-delete
     "helm.sh/hook-delete-policy": before-hook-creation, hook-succeeded, hook-failed
 rules:
@@ -29,7 +33,10 @@
 metadata:
   name: {{ include "spiffe-oidc-discovery-provider.fullname" . }}-pre-delete
   namespace: {{ include "spiffe-oidc-discovery-provider.namespace" . }}
+  labels:
+    {{- include "spiffe-oidc-discovery-provider.labels" . | nindent 4 }}
   annotations:
+    {{- include "spiffe-oidc-discovery-provider.patchedAnnotations" . | nindent 4 }}
     "helm.sh/hook": pre-delete
     "helm.sh/hook-delete-policy": before-hook-creation, hook-succeeded, hook-failed
 subjects:
@@ -49,6 +56,7 @@
   labels:
     {{- include "spiffe-oidc-discovery-provider.labels" . | nindent 4 }}
   annotations:
+    {{- include "spiffe-oidc-discovery-provider.patchedAnnotations" . | nindent 4 }}
     "helm.sh/hook": pre-delete
     "helm.sh/hook-delete-policy": before-hook-creation, hook-succeeded, hook-failed
 spec:
