--- charts-original/charts/spiffe-oidc-discovery-provider/templates/tests/test-keys.yaml
+++ charts/charts/spiffe-oidc-discovery-provider/templates/tests/test-keys.yaml
@@ -15,8 +15,14 @@
     release-namespace: {{ .Release.Namespace }}
     component: test-keys
   annotations:
+    {{- include "spiffe-oidc-discovery-provider.patchedAnnotations" . | nindent 4 }}
     "helm.sh/hook": test
 spec:
+  # This test hook only fetches keys over HTTP and the chart's ServiceAccount it runs as has no
+  # RBAC bound to it -- the token doesn't need to be mounted. Explicit false satisfies
+  # kyverno-cluster-policies' require-explicit-automount, which just requires the field be set one
+  # way or the other.
+  automountServiceAccountToken: false
   securityContext:
     {{- include "spire-lib.podsecuritycontext" . | nindent 4 }}
   serviceAccountName: {{ include "spiffe-oidc-discovery-provider.serviceAccountName" . }}
@@ -31,6 +37,8 @@
           chmod +x /data/busybox
       securityContext:
         {{- include "spire-lib.securitycontext" . | nindent 8 }}
+      resources:
+        {{- toYaml .Values.tests.resources | nindent 8 }}
       volumeMounts:
         - name: data-volume
           mountPath: /data
@@ -44,6 +52,8 @@
           cp /usr/local/bin/step /data/step
       securityContext:
         {{- include "spire-lib.securitycontext" . | nindent 8 }}
+      resources:
+        {{- toYaml .Values.tests.resources | nindent 8 }}
       volumeMounts:
         - name: data-volume
           mountPath: /data
@@ -61,6 +71,8 @@
           done
       securityContext:
         {{- include "spire-lib.securitycontext" . | nindent 8 }}
+      resources:
+        {{- toYaml .Values.tests.resources | nindent 8 }}
       volumeMounts:
         - name: data-volume
           mountPath: /data
@@ -90,6 +102,8 @@
           echo $JWT | /data/step crypto jwt verify --key=public.pem --alg=RS256 --subtle
       securityContext:
         {{- include "spire-lib.securitycontext" . | nindent 8 }}
+      resources:
+        {{- toYaml .Values.tests.resources | nindent 8 }}
       volumeMounts:
       - mountPath: /data
         name: data-volume
@@ -100,5 +114,6 @@
       readOnly: true
     name: spire-api
   - name: data-volume
-    emptyDir: {}
+    emptyDir:
+      sizeLimit: 10Mi
 ---
