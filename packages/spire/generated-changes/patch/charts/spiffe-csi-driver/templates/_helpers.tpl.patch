--- charts-original/charts/spiffe-csi-driver/templates/_helpers.tpl
+++ charts/charts/spiffe-csi-driver/templates/_helpers.tpl
@@ -74,6 +74,25 @@
 app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
 {{- end }}
 app.kubernetes.io/managed-by: {{ .Release.Service }}
+{{ include "spiffe-csi-driver.patchedLabels" . }}
+{{- end }}
+
+{{/*
+Org-required labels not otherwise emitted by this chart -- see kyverno-cluster-policies'
+config.requiredResourceLabels. Wired into spiffe-csi-driver.labels above so every resource that
+already renders that helper picks these up for free.
+*/}}
+{{- define "spiffe-csi-driver.patchedLabels" -}}
+metadata.example.com/team: spire
+{{- end }}
+
+{{/*
+Org-required annotations not otherwise emitted by this chart -- see kyverno-cluster-policies'
+config.requiredResourceAnnotations. Most resources here have no common-annotations helper to hook
+into, so this is included explicitly per-resource rather than transitively like patchedLabels.
+*/}}
+{{- define "spiffe-csi-driver.patchedAnnotations" -}}
+metadata.example.com/repository: https://github.com/therealgambo/k8s-charts
 {{- end }}
 
 {{/*
