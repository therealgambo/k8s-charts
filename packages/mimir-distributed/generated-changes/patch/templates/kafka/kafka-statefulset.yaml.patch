--- charts-original/templates/kafka/kafka-statefulset.yaml
+++ charts/templates/kafka/kafka-statefulset.yaml
@@ -8,7 +8,7 @@
   labels:
     {{- include "mimir.labels" (dict "ctx" $ "component" "kafka") | nindent 4 }}
   annotations:
-    {{- toYaml .podAnnotations | nindent 4 }}
+    {{- include "mimir.patchedAnnotations" (dict "ctx" $ "extra" .podAnnotations) | nindent 4 }}
   namespace: {{ $.Release.Namespace | quote }}
 spec:
   podManagementPolicy: Parallel
@@ -26,6 +26,7 @@
       namespace: {{ $.Release.Namespace | quote }}
     spec:
       serviceAccountName: {{ template "mimir.serviceAccountName" $ }}
+      automountServiceAccountToken: false
       {{- if .priorityClassName }}
       priorityClassName: {{ .priorityClassName }}
       {{- end }}
@@ -112,9 +113,11 @@
             failureThreshold: 3
       volumes:
         - name: kafka-config
-          emptyDir: {}
+          emptyDir: 
+            sizeLimit: 10Mi
         - name: tmp
-          emptyDir: {}
+          emptyDir: 
+            sizeLimit: 1Gi
         {{- if not .persistence.enabled }}
         - name: kafka-data
           emptyDir:
