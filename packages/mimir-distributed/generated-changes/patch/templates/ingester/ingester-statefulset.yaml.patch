--- charts-original/templates/ingester/ingester-statefulset.yaml
+++ charts/templates/ingester/ingester-statefulset.yaml
@@ -80,6 +80,7 @@
       schedulerName: {{ . | quote }}
       {{- end }}
       serviceAccountName: {{ template "mimir.serviceAccountName" . }}
+      automountServiceAccountToken: false
       {{- if .Values.ingester.priorityClassName }}
       priorityClassName: {{ .Values.ingester.priorityClassName }}
       {{- end }}
@@ -121,7 +122,8 @@
             name: {{ template "mimir.fullname" . }}-runtime
         {{- if not .Values.ingester.persistentVolume.enabled }}
         - name: {{ .Values.ingester.persistentVolume.name }}
-          emptyDir: {}
+          emptyDir: 
+            sizeLimit: 10Gi
         {{- end }}
         {{- if .Values.ingester.extraVolumes }}
         {{ toYaml .Values.ingester.extraVolumes | nindent 8 }}
@@ -130,7 +132,8 @@
         {{ toYaml .Values.global.extraVolumes | nindent 8 }}
         {{- end }}
         - name: active-queries
-          emptyDir: {}
+          emptyDir: 
+            sizeLimit: 1Gi
       containers:
         {{- if .Values.ingester.extraContainers }}
         {{ toYaml .Values.ingester.extraContainers | nindent 8 }}
