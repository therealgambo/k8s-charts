--- charts-original/templates/metamonitoring/metrics-instance-usernames-secret.yaml
+++ charts/templates/metamonitoring/metrics-instance-usernames-secret.yaml
@@ -8,6 +8,8 @@
   namespace: {{ $.Values.metaMonitoring.grafanaAgent.namespace | default $.Release.Namespace | quote }}
   labels:
     {{- include "mimir.labels" (dict "ctx" $ "component" "meta-monitoring") | nindent 4 }}
+  annotations:
+    {{- include "mimir.patchedAnnotations" (dict "ctx" $) | nindent 4 }}
 data:
   {{- range $i, $cfg := prepend (.additionalRemoteWriteConfigs | default list) .remote }}
   {{- if (($cfg).auth).username }}
