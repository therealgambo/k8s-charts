--- charts-original/templates/debug_config_map.yaml
+++ charts/templates/debug_config_map.yaml
@@ -4,6 +4,13 @@
 metadata:
   name: {{ include "yugabyte.fullname" . }}-master-hooks
   namespace: "{{ .Release.Namespace }}"
+  labels:
+    {{- include "yugabyte.applabel" (dict "label" "yb-master" "root" .) | indent 4 }}
+    {{- include "yugabyte.labels" . | indent 4 }}
+  annotations:
+    {{- if .Values.statefulSetAnnotations }}
+    {{- toYaml .Values.statefulSetAnnotations | nindent 4 }}
+    {{- end }}
 data:
 {{- range $index := until ( int ( .Values.replicas.master ) ) }}
   yb-master-{{.}}-pre_debug_hook.sh: "echo 'hello-from-pre' " 
@@ -15,6 +22,13 @@
 metadata:
   name: {{ include "yugabyte.fullname" . }}-tserver-hooks
   namespace: "{{ .Release.Namespace }}"
+  labels:
+    {{- include "yugabyte.applabel" (dict "label" "yb-tserver" "root" .) | indent 4 }}
+    {{- include "yugabyte.labels" . | indent 4 }}
+  annotations:
+    {{- if .Values.statefulSetAnnotations }}
+    {{- toYaml .Values.statefulSetAnnotations | nindent 4 }}
+    {{- end }}
 data:
 {{- range $index := until ( int ( .Values.replicas.tserver) ) }}
   yb-tserver-{{.}}-pre_debug_hook.sh: "echo 'hello-from-pre' " 
