--- charts-original/templates/distributor/service-distributor.yaml
+++ charts/templates/distributor/service-distributor.yaml
@@ -9,7 +9,11 @@
     {{- with .Values.distributor.service.labels }}
     {{- toYaml . | nindent 4 }}
     {{- end }}
-  {{- with .Values.distributor.service.annotations }}
+  annotations:
+    {{- include "tempo.patchedAnnotations" (dict "ctx" .) | nindent 4 }}
+  annotations:
+    {{- include "tempo.patchedAnnotations" (dict "ctx" .) | nindent 4 }}
+    {{- with .Values.distributor.service.annotations }}
   annotations:
     {{- tpl (toYaml . | nindent 4) $ }}
   {{- end }}
