--- charts-original/templates/_helpers.tpl
+++ charts/templates/_helpers.tpl
@@ -6,6 +6,14 @@
 {{- default .Chart.Name .Values.nameOverride | trunc 63 | trimSuffix "-" -}}
 {{- end -}}
 
+{{- define "kube-state-metrics.patchedLabels" }}
+metadata.example.com/team: demo-team
+{{- end }}
+
+{{- define "kube-state-metrics.patchedAnnotations" }}
+metadata.example.com/repository: https://github.com/therealgambo/k8s-charts
+{{- end }}
+
 {{/*
 Create a default fully qualified app name.
 We truncate at 63 chars because some Kubernetes name fields are limited to this (by the DNS naming spec).
@@ -62,6 +70,7 @@
 app.kubernetes.io/component: metrics
 app.kubernetes.io/part-of: {{ template "kube-state-metrics.name" . }}
 {{- include "kube-state-metrics.selectorLabels" . }}
+{{- include "kube-state-metrics.patchedLabels" . }}
 {{- if .Chart.AppVersion }}
 app.kubernetes.io/version: {{ .Chart.AppVersion | quote }}
 {{- end }}
