--- charts-original/templates/cilium-envoy/configmap.yaml
+++ charts/templates/cilium-envoy/configmap.yaml
@@ -7,14 +7,16 @@
 metadata:
   name: cilium-envoy-config
   namespace: {{ include "cilium.namespace" . }}
-  {{- with .Values.commonLabels }}
   labels:
+    app.kubernetes.io/name: cilium-envoy
+    {{- with .Values.commonLabels }}
     {{- toYaml . | nindent 4 }}
-  {{- end }}
-  {{- with .Values.envoy.annotations }}
+    {{- end }}
   annotations:
+    {{- include "cilium.patchedAnnotations" . | nindent 4 }}
+    {{- with .Values.envoy.annotations }}
     {{- toYaml . | nindent 4 }}
-  {{- end }}
+    {{- end }}
 data:
   # Keep the key name as bootstrap-config.json to avoid breaking changes
   bootstrap-config.json: |
