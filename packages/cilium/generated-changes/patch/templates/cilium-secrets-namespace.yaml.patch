--- charts-original/templates/cilium-secrets-namespace.yaml
+++ charts/templates/cilium-secrets-namespace.yaml
@@ -18,6 +18,7 @@
   name: {{ $name | quote }}
   labels:
     app.kubernetes.io/part-of: cilium
+    app.kubernetes.io/name: cilium
     helm.sh/chart: {{ include "cilium.chart" $ }}
     {{- with $.Values.commonLabels }}
     {{- toYaml . | nindent 4 }}
@@ -25,7 +26,12 @@
     {{- with $.Values.secretsNamespaceLabels }}
     {{- toYaml . | nindent 4 }}
     {{- end }}
+    # require-labels (kyverno-policy-fix): pager.example.com/key is
+    # kyverno-cluster-policies' config.requiredNamespaceLabels, checked on every Namespace this
+    # chart renders -- a separate key list from requiredResourceLabels above.
+    pager.example.com/key: pagerduty-demo
   annotations:
+    {{- include "cilium.patchedAnnotations" $ | nindent 4 }}
     {{- with $.Values.secretsNamespaceAnnotations }}
     {{- toYaml . | nindent 4 }}
     {{- end }}
