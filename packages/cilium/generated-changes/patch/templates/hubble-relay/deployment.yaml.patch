--- charts-original/templates/hubble-relay/deployment.yaml
+++ charts/templates/hubble-relay/deployment.yaml
@@ -4,10 +4,11 @@
 metadata:
   name: hubble-relay
   namespace: {{ include "cilium.namespace" . }}
-  {{- with .Values.hubble.relay.annotations }}
   annotations:
+    {{- include "cilium.patchedAnnotations" . | nindent 4 }}
+    {{- with .Values.hubble.relay.annotations }}
     {{- toYaml . | nindent 4 }}
-  {{- end }}
+    {{- end }}
   labels:
     k8s-app: hubble-relay
     app.kubernetes.io/name: hubble-relay
@@ -174,7 +175,10 @@
       {{- end }}
       volumes:
       - name: tmp-volume
-        emptyDir: {}
+        # require-emptydir-sizelimit (kyverno-policy-fix): no values knob reaches this volume
+        # upstream, so the sizeLimit is set directly here.
+        emptyDir:
+          sizeLimit: 100Mi
       - name: config
         configMap:
           name: hubble-relay-config
