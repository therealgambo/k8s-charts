--- charts-original/templates/_helpers.tpl
+++ charts/templates/_helpers.tpl
@@ -39,6 +39,17 @@
 {{- end -}}
 
 {{/*
+require-resource-annotations (kyverno-cluster-policies): annotation keys from
+config.requiredResourceAnnotations. This chart has no common-annotations helper of its own
+(unlike aws-ebs-csi-driver.labels below, which already threads .Values.customLabels through every
+resource that includes it) -- annotations are per-resource and often conditional here, so this is
+included explicitly wherever needed rather than folded into another helper.
+*/}}
+{{- define "aws-ebs-csi-driver.patchedAnnotations" }}
+metadata.example.com/repository: https://github.com/therealgambo/k8s-charts
+{{- end }}
+
+{{/*
 Common labels
 */}}
 {{- define "aws-ebs-csi-driver.labels" -}}
